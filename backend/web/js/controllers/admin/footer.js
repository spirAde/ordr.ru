(function() {
	'use strict';

	angular
		.module('adminPage.footer', [])
		.controller('footer', footer);

	footer.$inject = ['$scope', '$rootScope'];

	function footer($scope, $rootScope) {

	}
}());