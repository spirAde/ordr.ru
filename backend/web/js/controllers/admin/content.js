(function() {
	'use strict';

	angular
		.module('adminPage.content', [])
		.controller('content', content);

	content.$inject = ['$scope', '$rootScope'];

	function content($scope, $rootScope) {

	}
}());